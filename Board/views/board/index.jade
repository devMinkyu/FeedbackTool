extends ../layout

block content
  .container
    //- .main
    //-   a(href='/board')
    //-     h2 게시판임둥
    //-   table.board_list
    //-     tr
    //-       th No.
    //-       th Title
    //-       th Writer
    //-       th Date
    //-       th Count
    //-   .pagination
    //-   .btn_group
    //-     a(onclick='displayWriteForm();')
    //-       .write_btn 새 글
    //-     .search
    //-       form#searchAction(action='/boards/search', method='get', name='searchAction')
    //-         input#searchWord.search_word(type='text', name='searchWord')
    //-         a(href='#', onclick='searchContent();')
    //-           .search_btn 검색

    h1.page-header 게시판

    table.table.table-bordered
      thead
        tr
          th No.
          th Title
          th Writer
          th Date
          th Count
      tbody
        each board in boards
          tr
            td= board._id
            td
              a(href='/boards/#{board._id}')= board.title
            td= board.writer
            td= board.date
            td= board.count
    a.btn.btn-primary(href='/board/new') 게시판 작성하기